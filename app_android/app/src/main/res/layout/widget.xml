<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/widget_margin"
    android:background="@color/bg_layer">
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_ultimas_transacciones"
        android:layout_margin="4dp"
        android:id="@+id/textView"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:text="Inicia sesión para ver las transacciones"
        android:gravity="center_horizontal"
        android:textSize="20dp"
        android:id="@+id/widget_no_inicio_sesion"

        android:visibility="gone"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:text="@string/widget_no_transacciones"
        android:gravity="center_horizontal"
        android:textSize="20dp"
        android:id="@+id/widget_no_transacciones"
        android:visibility="gone"/>

    <ListView
        android:id="@+id/widget_lista"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/textView"
        android:layout_below="@+id/textView"
        ></ListView>
</RelativeLayout>